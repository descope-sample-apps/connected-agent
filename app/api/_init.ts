// Initialize all tools and ensure they're registered
// This file should be imported in any API route that needs access to tools

// Import the tools index to ensure all tools are registered
import "@/lib/tools";

console.log("API initialization complete - all tools registered");
